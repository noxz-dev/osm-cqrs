package config

const (
	DurationNodesReloading           = "duration for reloading nodes (in ms)"
	NumberOfReloadedNodes            = "number of reloaded nodes"
	DurationForRoutesFiltering       = "duration for Filtering for subject [" + RoutingSubject + "] (in ms)"
	DurationForSearchFiltering       = "duration for Filtering for subject [" + SearchSubject + "] (in ms)"
	DurationChangSetDownload         = "duration of download (in ms)"
	NumberOfIncomingElements         = "number of incoming elements"
	NumberOfPublishedElements        = "number of published elements for subject [" + AllSubject + "]"
	NumberOfPublishedSearchElements  = "number of published elements for subject [" + SearchSubject + "]"
	NumberOfPublishedRoutingElements = "number of published elements for subject [" + RoutingSubject + "]"
)
