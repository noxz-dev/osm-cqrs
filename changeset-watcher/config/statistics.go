package config

const (
	NumberOfReloadedNodes     = "number of reloaded nodes"
	DurationForFiltering      = "duration for specific-preprocessing (in ms)"
	NumberOfIncomingElements  = "number of incoming elements (reloaded + change-file)"
	NumberOfPublishedElements = "number of published elements"
	DurationTotal             = "duration for preprocessing (in ms)"
)
