package statistics

const (
	InsertDuration      = "duration of imposm insert (in ms)"
	WriteDuration       = "duration of writing zip file to disk (in ms)"
	SizeOfIncomingEvent = "size of incoming event (in bytes)"
)

const CollectStatistics = true
